[package]
name = "P4"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
# TLS dependencies for HTTPS server
tokio-rustls = "0.23"
rustls = "0.21"
rcgen = "0.9"
rustls-pemfile = "1.0"
